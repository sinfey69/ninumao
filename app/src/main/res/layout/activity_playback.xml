<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <androidx.media3.ui.PlayerView
        android:id="@+id/player_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:keepScreenOn="true"
        app:auto_show="false"
        app:show_timeout="3000" />

    <!-- 标题栏：随控制栏一起显示/隐藏 -->
    <LinearLayout
        android:id="@+id/title_overlay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_playback_top_gradient"
        android:orientation="vertical"
        android:paddingBottom="36dp"
        android:paddingEnd="24dp"
        android:paddingStart="24dp"
        android:paddingTop="20dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/overlay_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/overlay_progress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="#AAFFFFFF"
            android:textSize="14sp" />

    </LinearLayout>

    <!-- 底部视频列表：暂停或按向下键时显示，左右选择视频 -->
    <LinearLayout
        android:id="@+id/playlist_overlay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@drawable/bg_playback_bottom_gradient"
        android:orientation="vertical"
        android:paddingBottom="24dp"
        android:paddingTop="36dp"
        android:visibility="invisible">

        <TextView
            android:id="@+id/playlist_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:layout_marginBottom="8dp"
            android:text="@string/playback_playlist_hint"
            android:textColor="#AAFFFFFF"
            android:textSize="13sp" />

        <androidx.leanback.widget.HorizontalGridView
            android:id="@+id/playlist_grid"
            android:layout_width="match_parent"
            android:layout_height="@dimen/video_card_height"
            android:clipToPadding="false"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:paddingStart="24dp"
            android:paddingEnd="24dp" />

    </LinearLayout>

    <TextView
        android:id="@+id/error_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:visibility="gone" />

</FrameLayout>
